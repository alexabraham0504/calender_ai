/**
 * @fileoverview Frontend Type definitions for Calendar Image Integration
 * @module types/image
 * 
 * Re-exports shared types from root types directory for frontend use
 */

// Re-export all types for frontend
export type {
    CalendarImage,
    ImageCrop,
    ImageAlignment,
    ImageLayoutElement,
    CalendarView,
    CalendarImageLayout,
    PaperSize,
    PageOrientation,
    MarginPreset,
    CustomMargins,
    PrintSettings,
    PrintPDFRequest,
    AIImageGenerateRequest,
    ImageUploadMeta,
    ImageUploadResponse,
    SaveLayoutRequest,
} from '../../../types/image';
